server {
    host = "127.0.0.1"
    port = 8080
}
db {
    url = "jdbc:postgresql://localhost:54325/docker"
    user = "docker"
    password = "docker"
}
kafka {
    bootstrap-servers = "localhost:29092"
}